<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <!-- if group -->
    <div class="groups">
        <!-- group -->
        <div class="group {SYSTEM_CLASS} {SELECTED_CLASS}" hover="hover" group_id="{ID}" sortable="{SORTABLE}">
            <div class="buttons">
                {RENAME_BUTTON}
                {DELETE_BUTTON}
            </div>
            <div class="name">{NAME}</div>
        </div>
        <!-- / -->
    </div>
    <!-- / -->

    {CREATE_BUTTON}

</div>
