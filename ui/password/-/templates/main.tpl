<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="input">
        <input type="text" value="{PASSWORD}">
    </div>

    {GENERATE_BUTTON}

    <div class="cb"></div>

    {SET_BUTTON}

</div>
